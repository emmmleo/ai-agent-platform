<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.aiagent.agent.mapper.AgentConversationContextMapper">

    <resultMap id="AgentConversationContextResultMap" type="com.aiagent.agent.entity.AgentConversationContext">
        <id property="id" column="id"/>
        <result property="agentId" column="agent_id"/>
        <result property="userId" column="user_id"/>
        <result property="messages" column="messages"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <select id="findByAgentAndUser" resultMap="AgentConversationContextResultMap">
        SELECT id,
               agent_id,
               user_id,
               messages,
               created_at,
               updated_at
        FROM agent_conversation_context
        WHERE agent_id = #{agentId}
          AND user_id = #{userId}
    </select>

    <insert id="insert" parameterType="com.aiagent.agent.entity.AgentConversationContext" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO agent_conversation_context (agent_id, user_id, messages, created_at, updated_at)
        VALUES (#{agentId}, #{userId}, #{messages}, #{createdAt}, #{updatedAt})
    </insert>

    <update id="updateMessages">
        UPDATE agent_conversation_context
        SET messages = #{messages},
            updated_at = #{updatedAt}
        WHERE agent_id = #{agentId}
          AND user_id = #{userId}
    </update>

</mapper>
